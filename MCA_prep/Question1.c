// what is the output of this program?

#include <stdio.h>
int main() {
    int main = 3;

    printf("%d", main);
    return 0;
    // syntax error falls in compile time error, runtime error, logical error
}

